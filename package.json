{
  "name": "miniui",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "bun --hot dist/server.js",
    "build": "bun build src/index.js --outdir ./dist --minify --splitting",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "htm": "^3.1.0"
  }
}
